name: CD

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render deploy
        run: |
          curl -s --fail-with-body \
            "${{ secrets.RENDER_DEPLOY_URL }}"
